/* function scrollToSection(sectionId) {
    var section = document.getElementById(sectionId);
    var top = section.offsetTop;
    window.scrollTo(0, top);
  }
  
  // Enlaza el elemento `li` con la clase `head__enlace` a la sección `#seccion-servicios`
  document.querySelector(".head__enlace").addEventListener("click", function() {
    scrollToSection("seccion-servicios");
  });

  La función scrollTo() acepta dos parámetros: la posición x y la posición y a la que quieres desplazar la página. La posición x es la distancia horizontal desde el borde izquierdo de la ventana hasta la posición a la que quieres desplazar la página. La posición y es la distancia vertical desde el borde superior de la ventana hasta la posición a la que quieres desplazar la página.

  */